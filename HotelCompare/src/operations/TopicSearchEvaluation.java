package operations;

/**
 * @author Administrator search through the comment by breaking it down to
 *         sentences and in each of them look for the topic in hand and the
 *         semantic words that it may be associated with.
 */
public class TopicSearchEvaluation {

	public void sentenceBreak(String comment, String topic) {
		// TODO get every line of the comment,

	}

	public void wordBreak(String sent, String topic) {
		// TODO get every word of the line searching for the specified topic
		// with the semantic emphasis keywords
	}
}
